<%= form_for(board_message) do |form| %>
  <% if board_message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(board_message.errors.count, "error") %> prohibited this board_message from being saved:</h2>

      <ul>
        <% board_message.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

<table>
  <tr class="field">
    <th><%= form.label :content %></th>
    <td><%= form.text_area :content, cols:40, row:3 %></td>
  </tr>

  <tr class="field">
    <th><%= form.label :board_user_id %></th>
    <td><%= @board_user.nickname %><%= form.hidden_field :board_user_id %></td>
  </tr>

  <tr class="actions">
    <th></th><td><%= form.submit %></td>
  </tr>
</table>
<% end %>
