<%= form_for(board_user) do |form| %>
  <% if board_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(board_user.errors.count, "error") %> prohibited this board_user from being saved:</h2>

      <ul>
        <% board_user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <table>
  <tr class="field">
    <th><%= form.label :nickname %></th>
    <td><%= form.text_area :nickname %></td>
  </tr>
  <%= form.hidden_field :account_id %>

  <tr class="field">
    <th><%= form.label :memo %></th>
    <td><%= form.text_area :memo, cols:40, row:3 %></td>
  </tr>

  <tr class="actions">
    <th></th><td><%= form.submit %></td>
  </tr>
  </table>
<% end %>
